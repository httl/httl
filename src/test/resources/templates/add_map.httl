<!--#var(Map<String, Book> bookmap, Map<String, Book> bookmap2)-->
<table>
  <!--#foreach(bookentry in bookmap + bookmap2)-->
  <!--#set(Book book = bookentry.value)-->
  <tr>
    <td>${foreach.count}</td>
    <td>${bookentry.key}</td>
    <td>${book.author}</td>
    <td>${book.publisher}</td>
    <td>${book.publication}</td>
    <td>${book.price}</td>
    <td>${book.discount}%</td>
    <td>${book.price * book.discount / 100}</td>
  </tr>
  <!--#end-->
</table>